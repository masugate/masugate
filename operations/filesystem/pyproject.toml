[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "masugate-operation-filesystem"
version = "0.1.1"
description = "Bounded protected filesystem operation pack for MasuGate"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }

dependencies = ["masugate==0.1.1", "masugate-connector-sdk==0.1.1"]

[project.urls]
Homepage = "https://github.com/masugate/masugate"
Source = "https://github.com/masugate/masugate"
Issues = "https://github.com/masugate/masugate/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/masugate_operation_filesystem"]

[tool.hatch.build.targets.wheel.force-include]
"src/masugate_operation_filesystem/operation-pack.json" = "masugate_operation_filesystem/operation-pack.json"
