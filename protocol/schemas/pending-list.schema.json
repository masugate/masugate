{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.masugate.invalid/protocol/schemas/pending-list.schema.json",
  "title": "Pending operation list",
  "description": "A snapshot of unresolved operations and the durable cursor immediately after that snapshot.",
  "type": "object",
  "additionalProperties": false,
  "required": ["items", "next_cursor"],
  "properties": {
    "items": {
      "type": "array",
      "items": {"$ref": "#/$defs/pending_operation"}
    },
    "next_cursor": {
      "type": "string",
      "minLength": 1
    }
  },
  "$defs": {
    "decision": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "effect",
        "policy_id",
        "policy_version",
        "rule_id",
        "reason"
      ],
      "properties": {
        "effect": {"const": "escalate"},
        "policy_id": {"type": "string", "minLength": 1},
        "policy_version": {"type": "string"},
        "rule_id": {"type": "string", "minLength": 1},
        "reason": {"type": "string"}
      }
    },
    "pending_operation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "pending_id",
        "operation_id",
        "principal_id",
        "action",
        "args",
        "created_at",
        "decision",
        "audit_ref"
      ],
      "properties": {
        "pending_id": {"type": "string", "format": "uuid"},
        "operation_id": {"type": "string", "format": "uuid"},
        "principal_id": {"type": "string", "minLength": 1},
        "action": {"type": "string", "minLength": 1},
        "args": {"type": "object"},
        "created_at": {"type": "string", "format": "date-time"},
        "resolution_plan": {
          "enum": ["revalidate", "scoped-hold", "reservation-proof"]
        },
        "reservation_safety_certificate_digest": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "reservation_entitlement_digest": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "decision": {"$ref": "#/$defs/decision"},
        "audit_ref": {
          "type": "string",
          "pattern": "^/v1/audit/[^/]+$"
        }
      },
      "oneOf": [
        {
          "required": [
            "resolution_plan",
            "reservation_safety_certificate_digest",
            "reservation_entitlement_digest"
          ],
          "properties": {
            "resolution_plan": {"const": "reservation-proof"}
          }
        },
        {
          "required": ["resolution_plan"],
          "properties": {
            "resolution_plan": {"enum": ["revalidate", "scoped-hold"]},
            "reservation_safety_certificate_digest": false,
            "reservation_entitlement_digest": false
          }
        },
        {
          "properties": {
            "resolution_plan": false,
            "reservation_safety_certificate_digest": false,
            "reservation_entitlement_digest": false
          }
        }
      ]
    }
  }
}
