# Verified implementation artifact for the bounded MAF profile. Install this
# file with ``pip install --no-deps --require-hashes -r requirements.txt``
# before installing this package so the MAF wheel itself is byte-pinned.
agent-framework-core==1.12.0 \
    --hash=sha256:71716a2c109b7ca89aaa221796876dc7166fed741d3fa75c967f8f24d02c6ead
