# Provenance

The `0.1.0` research-preview release is derived from the fixed source snapshot
`1373f5507c1680c60a7700d8a6c26a8b4d3fb025`. Its release descriptor is
[`release/reference-release.json`](release/reference-release.json).

The release builder writes checksums, a CycloneDX SBOM, and machine-readable
provenance for each sealed artifact set. Those generated records bind the
source snapshot, the public-tree revision, declared inputs, and artifact
digests without requiring a private workspace path or repository history.

Version: `0.1.0` (research preview). Next: [Third-party notices](THIRD_PARTY_NOTICES.md).
